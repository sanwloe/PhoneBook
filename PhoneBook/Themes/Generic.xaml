<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.modernwpf.com/2019">

    <Style x:Key="styleNumbersDataGrid" TargetType="DataGrid" BasedOn="{ui:StaticResource ResourceKey=DefaultDataGridStyle}">
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="CanUserResizeColumns" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>

        <Setter Property="ColumnHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridColumnHeader" BasedOn="{ui:StaticResource ResourceKey=DefaultDataGridColumnHeaderStyle}">
                    <Setter Property="BorderBrush" Value="Plum"/>
                </Style>
            </Setter.Value>
        </Setter>
        
        <Setter Property="RowStyle">
            <Setter.Value>
                <Style TargetType="DataGridRow" BasedOn="{ui:StaticResource ResourceKey=DefaultDataGridRowStyle}">
                    <Setter Property="Cursor" Value="Hand"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>